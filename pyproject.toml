[project]
name = "aoa-models"
version = "0.1.0"
description = "Configuration-scoped, lifecycle-aware model knowledge for AoA"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.23,<5"]

[tool.setuptools]
packages = []

[tool.ruff]
line-length = 100
target-version = "py311"
